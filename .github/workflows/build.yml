name: Build EXE

on:
  workflow_dispatch:   # <-- ЭТО ОБЯЗАТЕЛЬНО
    inputs:
      user_id:
        description: 'Telegram User ID'
        required: true
        type: string
      discord_webhook:
        description: 'Discord Webhook (optional)'
        required: false
        type: string
      file_name:
        description: 'Desired EXE file name (without .exe)'
        required: false
        type: string
        default: 'benz0'
      icon_base64:
        description: 'Icon file as base64 (optional)'
        required: false
        type: string

jobs:
  build:
    runs-on: windows-latest
    steps:
      # ... остальные шаги
